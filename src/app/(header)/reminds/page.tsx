'use client';

import classNames from 'classnames';
import React from 'react';
import './index.scss';

export default function RemindPage() {
  return (
    <div className={classNames(['remind-page'])}>
      <div className={classNames(['remind-page__title'])}>리마인드</div>
    </div>
  );
}
